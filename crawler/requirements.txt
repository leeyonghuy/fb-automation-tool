yt-dlp>=2024.1.0
google-auth>=2.0.0
google-auth-oauthlib>=0.4.0
google-api-python-client>=2.0.0

# Config / utils chung
python-dotenv>=1.0.0
imageio-ffmpeg>=0.4.9   # locate ffmpeg binary qua Python
requests>=2.31.0        # XHS HTTP tier 1
playwright>=1.40.0      # XHS tier 2 (cookie refresh) + dùng chung

# Video Editor - Tầng 2 & 3
google-generativeai>=0.5.0   # Gemini API (dịch + metadata) - chính
edge-tts>=6.1.0              # TTS tiếng Việt (lồng tiếng)

# Fallback offline pipeline (douyin_translate_tts.py)
openai-whisper>=20231117     # Whisper local
deep-translator>=1.11.4      # Google Translate fallback
