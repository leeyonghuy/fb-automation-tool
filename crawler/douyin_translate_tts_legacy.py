"""
douyin_translate_tts_legacy.py

Đây là backup của file `douyin_translate_tts.py` cũ — script một lần
hardcode cho 1 video Douyin cụ thể. Logic dịch (Whisper local) vẫn còn
giá trị tham khảo nếu sau này muốn fallback offline.

Để chạy thật, dùng `crawler/video_editor.py::apply_translation_layer`.
"""
